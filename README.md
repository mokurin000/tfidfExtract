# TF-IDF Extract

## Installation

```bash
pip install -e .
```

## Input structure

`<>` means required, `[]` means unused but supported

```
data/
    xxxxx/
        <stock>_<year>_<name>[_*].txt
```